<section id="fix-scope-panel" class="panel workflow-panel" aria-labelledby="scope-heading" hidden>
  <div class="section-heading section-heading-compact">
    <div>
      <p class="eyebrow">03 · Scope</p>
      <h2 id="scope-heading">Fix scope</h2>
    </div>
    <button id="scope-clear" class="button button-quiet" type="button">Clear</button>
  </div>
  <p class="section-copy">The agent can propose a bounded scope. You keep the same visible selection and decide what gets implemented.</p>
  <ol id="scope-list" class="scope-list"></ol>
  <div class="panel-actions">
    <button id="verify-scope" class="button button-primary" type="button" disabled>Re-audit &amp; verify</button>
    <span id="scope-count" class="muted-label">0 selected</span>
  </div>
</section>
